import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    containerPositionName: {
        alignItems: "center",
        margin: 20,
    },
    playerContainer: {
        backgroundColor: "#a76f63",
        padding: 30,
        marginBottom: 10,
    },
    playerContent: {
        flexWrap: "wrap",
        justifyContent: "space-between",
        flexDirection: "row",
    },
});

export default styles;